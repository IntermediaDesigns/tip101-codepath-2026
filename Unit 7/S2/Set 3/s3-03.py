# ------------------------------------------------
#  *                    Problem 3: Where Does it Go (Recursive)
#
#    Rewrite the search_insert() function from Problem 2 recursively.
#
#    Given a sorted array of distinct integers and a target value, return the index if
#    the target is found. If not, return the index where it would be inserted in order.
#
#    You must write an algorithm with O(log n) runtime complexity.


def search_insert_recursive(nums, target):
    pass


# Example Input: nums = [1, 3, 5, 6], target = 5
# Example Output: 2
# Explanation: 5 has index 2 in the list
#
# ------------------------------------------------
