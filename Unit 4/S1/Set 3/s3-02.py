# ------------------------------------------------
#  *                    Problem 2: Two-Pointer Target Sum
#
#    Write a function two_sum() that takes in a sorted list of integers nums and
#    an integer target as parameters and returns the indices of the two numbers
#    that add up to target. Each input has exactly one solution, and you may not
#    use the same element twice. Return the indices in any order.
#    The function must use the two-pointer approach.


def two_sum(nums, target):
    pass


nums = [2, 7, 11, 15, 17]
sol1 = two_sum(nums, 9)
print(sol1)

sol2 = two_sum(nums, 18)
print(sol2)

# Example Output:
# [0, 1]
# [1, 2]
#
# ------------------------------------------------
