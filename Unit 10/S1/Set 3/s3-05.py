# ------------------------------------------------
#  *                    Problem 5: Two Sum II
#
#    Given a sorted (non-decreasing) array of integers numbers, return the
#    0-indexed indices [index1, index2] of two numbers that add up to target,
#    where index1 < index2. There is always exactly one valid solution.
#    Your solution must use O(1) additional space.

def two_sum(numbers, target):
    pass


print(two_sum([1, 2, 3, 4], 3))
# Expected Output: [0, 1]
# numbers[0] + numbers[1] = 1 + 2 = 3

# ------------------------------------------------
